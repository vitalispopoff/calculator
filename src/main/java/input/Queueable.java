package input;

/**
 * Represents a queue of a parsed expressions forming an equation.
 * *
 */
public interface Queueable {
}
// ? TODO section below is disposable, innit ?

/**
 * enables node to enter at the end
 * of a queue
 * <p>
 * enables the node in front of the queue
 * to leave it
 * <p>
 * returns the current lenght of a queue
 */
//    void joinQueue (Queueable queue);

/**
 * enables the node in front of the queue
 * to leave it
 * */
//    void leaveQueue (Queueable queue);

/**
 * returns the current lenght of a queue
 * */
//    int queueLength();

