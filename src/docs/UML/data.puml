@startuml

package data {

    +interface Queueable {

    }

    +interface Treeable extends Queueable{

    }

    +interface Nodeable extends Treeable{

    }

    +class Queue {

    }

    +class Node {

    }

    Queue       -right-*    Queueable
    Node        -right-*    Nodeable
}

    () Solvable
    () Parsable

    Queue       -right-|>   Solvable
    Node        -right-|>   Parsable
    Queue       ---*     Node

@enduml